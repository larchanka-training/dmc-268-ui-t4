/**
 * Placeholder shell. The router, providers and the composition root land here
 * in the architecture PR.
 */
export function App() {
  return (
    <main className="mx-auto flex max-w-2xl flex-col gap-2 p-8">
      <h1 className="text-2xl font-semibold">DMC-268 Team 4 UI</h1>
      <p className="text-content-muted">React + TypeScript + Vite</p>
    </main>
  );
}
